/** Cookie name for Bungie OAuth CSRF `state` (must match login + callback). */
export const BUNGIE_OAUTH_STATE_COOKIE = "armor_checklist_oauth_state";

export const BUNGIE_OAUTH_STATE_TTL_SECONDS = 10 * 60;
